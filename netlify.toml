[build]
  command = "npm run build"
  publish = "dist"

# Proxy all /api requests to your Render backend
[[redirects]]
  from = "/api/*"
  to = "https://triage-q04x.onrender.com/:splat"
  status = 200
  force = true
